import React, { useState } from 'react';

// Parte Superior

const InTheBeginning = () => {
  // Começo das funções React
  const [isABoolean, setIsABoolean] = useState(false);
  const [aString, setAString] = useState('');

  return <div />;
};

export default InTheBeginning;
